# Copy this file to .env and fill in ONE of the two keys below.
# llm_client.py auto-detects whichever is present (OpenAI is tried first).

OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional: override default model names
OPENAI_MODEL=gpt-4o-mini
GEMINI_MODEL=gemini-1.5-flash
