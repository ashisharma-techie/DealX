# ai-engine

Owner: Person 3 (AI Intelligence, Predictive Models & NLP)

Forecasting, Deal Integrity Score, spec-matching, and negotiation-bot logic for DealX. This folder is currently empty and will be scaffolded by Person 3 using their dedicated prompt (see the team's DealX Claude Code Prompts doc).

Planned structure:
- `forecasting/` — 30-day price forecasting model (Prophet / scikit-learn)
- `deal_integrity/` — Deal Integrity Score algorithm
- `llm_matching/` — LLM-based cross-marketplace spec matching
- `negotiation_bot/` — draft P2P negotiation message logic
- `notebooks/` — scratch/exploration notebooks

Integrates with `backend/` either as a direct Python import or a small internal microservice — see this folder's own README (once scaffolded) for which approach was chosen.
